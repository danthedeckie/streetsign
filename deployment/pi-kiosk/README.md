# Raspberry pi basic deployment script(s)
The files in here are helper files for setting up a raspberry pi as a 'web kiosk'
full screen web browser for displaying a streetsign instance, not the streetsign
instance itself.


